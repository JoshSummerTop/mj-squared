<%# Success: Show flash message, then close modal and redirect %>

<%# Show success flash message first %>
<%= turbo_stream.flash_message "Post '#{@post.title}' created successfully!", type: "success", duration: 6000 %>

<%# Then close modal and redirect after a brief delay %>
<%= turbo_stream.redirect_to_path post_path(@post), delay: 1000 %>

